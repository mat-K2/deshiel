%h1 Listing entries

%table
  %tr
    %th UserId
    %th Description
    %th
    %th
    %th

  - current_user.pupil_entries_by_master(master).each do |entry|
    %tr
      %td= entry.user.id
      %td= entry.description
      %td= link_to 'Destroy', pupil_entry_path(entry), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Entry', new_pupil_entry_path(params: {master_id: master.id})
