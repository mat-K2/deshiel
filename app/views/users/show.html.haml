%h1
  = @user.email

= "#{current_user.email}でログイン中"
= link_to '自身の問題一覧', questions_path
= link_to 'ログアウト', destroy_user_session_path, :method => :delete

%p
  他のメンバー
  - User.all.each do |user|
    = link_to user.email, user_path(user.id)